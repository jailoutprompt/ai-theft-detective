services:
  - type: web
    name: ai-theft-detective
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python3 scripts/build_cctv_db.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
